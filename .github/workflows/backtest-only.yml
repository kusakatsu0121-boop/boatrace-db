name: Fast leakage-safe EV backtest

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout model code
        uses: actions/checkout@v4
      - name: Checkout public boat-race data
        uses: actions/checkout@v4
        with:
          repository: BoatraceCSV/boatracecsv.github.io
          path: source
          fetch-depth: 1
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy
      - name: Run OOS EV backtest
        run: python backtest_ev.py --source source/data --out artifacts/backtest_ev | tee /tmp/backtest.log
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: ev-backtest
          path: artifacts/backtest_ev/*.csv
          retention-days: 30
      - name: Publish result
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python - <<'PY' > /tmp/result.md
          import pandas as pd
          s=pd.read_csv('artifacts/backtest_ev/summary.csv')
          meta=pd.read_csv('artifacts/backtest_ev/fold_meta.csv')
          primary=s[s['strategy'].eq('EV1.15_TOP1')]
          print('Leakage-safe EV backtest completed.')
          print('')
          print('Primary: EV >= 1.15, max 1 bet/race, fixed 100 yen.')
          print('```')
          print(primary.to_string(index=False))
          print('```')
          print('')
          print('Fold meta:')
          print('```')
          print(meta.to_string(index=False))
          print('```')
          PY
          gh issue create --title "EV backtest result ${GITHUB_RUN_ID}" --body-file /tmp/result.md
