name: motto-takakunai-regression

on:
  push:
    branches: [main]
    paths:
      - 'motto-takakunai/**'
      - '.github/workflows/motto-takakunai-tests.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: motto-takakunai
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test
